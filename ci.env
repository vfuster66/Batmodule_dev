# Configuration pour les tests CI/CD
NODE_ENV=test
DATABASE_URL=postgresql://test:test@localhost:5432/batmodule_test
JWT_SECRET=test-secret-key-for-ci-cd-pipeline
ENCRYPTION_KEY=test-encryption-key-32-chars-long-for-ci
PORT=3000

# Configuration backend pour tests
BACKEND_URL=http://localhost:3000
FRONTEND_URL=http://localhost:5173

# Configuration de sécurité pour tests
CSRF_SECRET=test-csrf-secret-for-ci
SESSION_SECRET=test-session-secret-for-ci

# Configuration email pour tests (mock)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=test
SMTP_PASS=test
SMTP_FROM=test@batmodule.com

# Configuration pour les tests d'intégration
TEST_TIMEOUT=10000
TEST_DATABASE_RESET=true
